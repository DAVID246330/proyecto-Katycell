<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalle del Producto - Katicell</title>
  <link
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="/css/detalle-prod.css">
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/home">Inicio</a></li>
      </ul>
    </nav>
  </header>

  <div class="container">
    <h1>Detalles del Producto</h1>

    <div class="product-details">
      <div class="product-image">
        <img
          src="<%= producto.imagen %>"
          alt="<%= producto.nombre %>"
          style="width: 100%; height: auto;"
        >
      </div>
      <div class="product-info">
        <h2><%= producto.nombre %></h2>
        <p><%= producto.descripcion %></p>
        <p class="price">
          <strong>
            $<%= Number(producto.precio).toLocaleString('es-CO', { minimumFractionDigits: 2 }) %>
          </strong>
        </p>
        <% if (producto.precio_descuento != null) { %>
          <p id="product-discount-price">
            Ahora: $
            <%= Number(producto.precio_descuento).toLocaleString('es-CO', { minimumFractionDigits: 2 }) %>
          </p>
        <% } %>
        <p>Stock disponible: <%= producto.stock %></p>

        <!-- Botón de agregar al carrito en página de detalle -->
        <button
          class="add-to-cart-btn"
          onclick="agregarAlCarrito(
            event,
            '<%= producto.id_producto %>',
            '<%= producto.nombre %>',
            '<%= producto.precio %>',
            '<%= producto.imagen %>'
          )"
        >
          Agregar al carrito
        </button>
      </div>
    </div>

    <a href="javascript:history.back()" class="back-btn">
      <i class="fas fa-arrow-left"></i> Volver
    </a>
  </div>
</body>
</html>

